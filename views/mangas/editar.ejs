<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <title>Registro de mangas</title>
</head>

<body>
    <div class="container">
       <%- include ('../partials/navbar'); %>
       <div class="card">
           <div class="card-body">
               <h3 class="card-title">Edição de um manga</h3>
                    <form action="/mangas/atualizar" method="POST">
                        <div class="form-group">
                            <label for="nome">Nome do manga</label>
                            <input class="form-control" type="text" id="nome" name="nome" value="<%=manga.nome%>"
                                    placeholder="Informe o nome do manga" required> <br>
                        </div>
                        <div class="form-group">
                            <label for="capitulo">Capítulo</label>
                            <input class="form-control" type="number" id="capitulo" name="capitulo"  value="<%=manga.capitulo%>"
                                    placeholder="Informe o capitulo do manga" required> <br>
                        </div>

                        <div class="form-group">
                            <label for="capitulo">Status</label>
                            <select class="form-control" name="status" id="status">
                                <option value="lendo">lendo</option>
                                <option value="finalizado">finalizado</option>
                                <option value="ler futuramente">ler futuramente</option>    
                            </select>
                        </div>

                        <div class="form-group">
                            <label for="lancamento">Lançamento</label>
                            <input class="form-control" type="date" id="lancamento" name="lancamento" value="<%=manga.lancamento%>"> 
                            
                        </div><div class="form-group">
                            <label for="categoria">Categorias</label>
                            <select class="form-control" id="categoria" name="categoria">

                                <% categorias.forEach(function(categoria){ 
                                     if(categoria.id == manga.categoriaId){ %>
                                        <option value="<%=categoria.id%>" selected><%=categoria.nome%></option>
                                    <% }     
                                    else { %>
                                        <option value="<%=categoria.id%>"><%=categoria.nome%></option>
                                    <% } %>
                                <% }) %>

                            </select><br>
                        </div>
                        <input type="hidden" name="id" value="<%=manga.id%>">
                        <input class="btn btn-outline-success" type="submit" value="Atualizar">
                        <a href="/mangas" class="btn btn-outline-dark">Lista de mangas</a>
                    </form>
           </div>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"
        integrity="sha384-DfXdz2htPH0lsSSs5nCTpuj/zy4C+OGpamoFVy38MVBnE+IbbVYUew+OrCXaRkfj"
        crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js"
        integrity="sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo"
        crossorigin="anonymous"></script>
    <script src="/js/bootstrap.min.js"></script>
</body>

</html> 